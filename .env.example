DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=

# global (production) or guild (faster local iteration)
DISCORD_COMMAND_SCOPE=global

# Required only when DISCORD_COMMAND_SCOPE=guild
DISCORD_GUILD_ID=

# Optional default; can be overridden per command with import_token
# Must match backend DISCORD_IMPORT_TOKEN on the target server
DISCORD_IMPORT_TOKEN=

# Optional default target server; can be overridden per command with base_url
ZENTRA_BASE_URL=http://localhost:8080
